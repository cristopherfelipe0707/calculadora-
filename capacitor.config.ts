import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'calculadora-perimetros-vscode',
  webDir: 'www'
};

export default config;
